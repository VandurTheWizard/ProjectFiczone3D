using UnityEngine;

public class Prueba : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        LeaderBoardGestions.activateLeaderBoardNotTime("eggsss", 55);   
    }
}
